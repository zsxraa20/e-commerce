craete database: 

CREATE DATABASE ephone;
USE ephone;

CREATE TABLE users (
id INT AUTO_INCREMENT PRIMARY KEY,
email VARCHAR(100),
password VARCHAR(255),
role ENUM('admin','user') DEFAULT 'user'
);

create user admin: 

INSERT INTO users (email,password,role)
VALUES ('admin@ephone.com','admin123','admin');

create user for databse:

CREATE USER 'ephoneuser'@'localhost' IDENTIFIED BY '123456';
GRANT ALL PRIVILEGES ON ephone.* TO 'ephoneuser'@'localhost';
FLUSH PRIVILEGES;

add username database:

ALTER TABLE users ADD username VARCHAR(50);